# Agent contract for ai-fanout.com

Before planning, writing, redesigning, publishing, or materially changing this site:

1. Read `README.md` and `src/data/site.ts` completely.
2. On Matthias's machine, validate `C:\Users\matth\Documents\DomainPortfolio` and read `strategy/domains/ai-fanout.com.md` plus `strategy/projects/contextter.md` completely.
3. Read the current owned-domain recovery handoff at `C:\Users\matth\Documents\DomainPortfolio\reports\owned-domain-rebuild\domains\ai-fanout.com.md` and its referenced raw evidence JSON.
4. Treat the recorded purpose, identity disclosure, rights constraints, linking policy, operating burden, and stop condition as hard context.
5. Do not copy former-site text, code, media, downloads, authorship, users, customers, subscribers, or identity without documented rights.
6. Do not present a commonly owned portfolio site as independent corroboration.
7. Keep every route `noindex` until the real site has explicit launch approval and verified production evidence.
8. Keep `manifests/rights-and-sources.v1.json` and `manifests/legacy-url-actions.v1.json` aligned with implementation and verification evidence.
9. After a material implementation or deployment change, update the canonical DomainPortfolio with the manager scripts and validate it.

Domain-specific hard boundary:

The site must not claim access to hidden queries, private retrieval traces or model reasoning. It stays noindex until the launch gate is met.
