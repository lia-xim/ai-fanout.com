# Agent contract for ai-fanout.com

Before planning, writing, redesigning, publishing, or materially changing this site:

1. Read `README.md`, `PROJECT_BRIEF.md` and `src/data/site.ts` completely.
2. On Matthias's machine, validate `C:\Users\matth\Documents\DomainPortfolio` and read `strategy/domains/ai-fanout.com.md` plus `strategy/projects/contextter.md` completely.
3. Treat the recorded purpose, identity disclosure, linking policy, operating burden and stop condition as hard context.
4. ai-fanout.com is a fresh registration by Matthias Ramahi for this project. Current product routes and unknown-path behavior are governed by `manifests/route-actions.v1.json`.
5. Third-party screenshots, copied text, stored model outputs and external assets may be published only with verified provenance, attribution and applicable rights.
6. Browser-local user inputs are not transmitted by ai-fanout.com. Keep the privacy notice aligned with the actual Lab, export, analytics, font, form and storage implementation.
7. Do not present a commonly owned portfolio site as independent corroboration.
8. Keep every route `noindex` until the real site has explicit launch approval and verified production evidence. A public preview remains crawlable through `robots.txt`; a confidential preview requires real access protection.
9. Keep `manifests/rights-and-sources.v1.json` and `manifests/route-actions.v1.json` aligned with implementation and verification evidence.
10. After a material implementation or deployment change, update the canonical DomainPortfolio with the manager scripts and validate it.

Domain-specific hard boundary:

The site must not claim access to hidden queries, private retrieval traces or model reasoning. It stays noindex until the launch gate is met.
