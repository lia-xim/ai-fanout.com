---
import ExamplePage from "../../components/ExamplePage.astro";
import { englishExamples,type ExamplePage as ExamplePageData } from "../../data/examples";
export function getStaticPaths(){return englishExamples.map(page=>({params:{slug:page.slug},props:{page}}))}interface Props{page:ExamplePageData}const{page}=Astro.props;
---
<ExamplePage page={page} lang="en" />
